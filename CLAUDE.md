# CLAUDE.md — 그린프로 홈페이지 (Claude Code 작업 지침)

이 파일은 Claude Code가 이 프로젝트를 이어받아 작업할 때 **먼저 읽는 규칙서**입니다.
사이트는 이미 5개 페이지가 완성되어 있습니다. 처음부터 다시 만들지 말고, **기존 코드를 이어서 다듬고 배포**하세요.

## 프로젝트 개요
- **회사**: ㈜그린프로 (GREENPRO) — 태양광 신재생에너지 **정부지원사업 전문 컨설팅**.
- **한 줄**: 배출권거래제 할당대상업체를 위해, 태양광 지원사업을 신청부터 20년 관리까지 대행하는 밀착 컨설팅.
- **주 타깃**: 배출권거래제 할당대상업체(중소·중견). 확대: 배출권 비해당 기업.
- **목적**: B2B 리드(무료 상담) 수집.

## 반드시 지킬 것 (Rules)
- **메인 컬러는 블루 `#0049F0`** (회사명은 '그린'이지만 시각 정체성은 블루). 그린 계열로 바꾸지 말 것.
- **폰트는 Pretendard** (`assets/css/style.css`에서 CDN import). **검은고딕(Black Han Sans) 사용 금지** — 숫자 포함 전부 Pretendard.
- **"규모가 작아 보이지 않게"** — 넓은 여백, 큰 타이포, 실적 숫자(20,000kW·20년)를 전면에. "중소·작은" 류 표현 노출 금지.
- **완전 반응형** 유지 (모바일 우선). 기존 `@media` 브레이크포인트(900/640px) 준수.
- **AI스러운 디자인 금지**: 보라 그라디언트, 과한 그림자, 둥근 모서리 남발, 이모지 장식 등.
- 디자인 톤: 신뢰·전문·안심·전담. (참고 감각: Auxia처럼 모던·미니멀 + 얇은 라인/큰 워드마크 — 이미 반영됨)

## 기술 스택 / 구조
- **순수 정적 사이트** (HTML/CSS/JS, 프레임워크 없음). **Vercel** 배포.
- 페이지: `index.html`(홈) · `about.html`(회사소개) · `business.html`(사업영역) · `record.html`(실적·사례) · `contact.html`(문의) · `privacy.html`(개인정보처리방침).
- 공통: `assets/css/style.css`, `assets/js/main.js`. 헤더/푸터는 각 페이지에 인라인(정적 사이트라 중복). 수정 시 **모든 페이지에 동일하게 반영**할 것.
- 이미지: `assets/img/` (현재 시공 사진은 `[ 시공 사진 ]` 플레이스홀더 — 사진 확보 시 교체).

## 회사 정보 (푸터·문의·개인정보처리방침 공통)
- ㈜그린프로 / 대표전화 010-2293-7342 / info@greenpro.co.kr
- 주소: 충남 천안시 서북구 2공단1로 17 천안자이타워 지식산업센터 1105호
- 사업자등록번호: 791-81-00084

## 아직 안 된 것 (해야 할 일)
1. **문의 폼 실제 전송 연결** — `assets/js/main.js`의 `FORM_ENDPOINT`에 폼 서비스(예: Formspree) 주소 입력. 지금은 프론트 "접수 완료" 안내만 뜸. → info@greenpro.co.kr로 도착하게.
2. **시공 사진 교체** — `record.html`·`index.html`의 `[ 시공 사진 ]` 자리에 실제 사진.
3. **SEO 마무리** — 페이지별 `<title>`/`description`/OG는 넣어둠. 추가로 favicon, `sitemap.xml`, `robots.txt`. **"그린프로" 검색 1위 자산 보존**이 중요 — 기존 URL/메타 크게 바꾸지 말 것.
4. **개인정보처리방침 최종화** — 보호책임자 성명·직책, 공고/시행일.
5. (선택) Google Analytics.
6. **배경 곡선(감각적 요소)** — 참고용 코드는 별도 보관(2안 원본). 원하면 재적용 가능.

## 미리보기(로컬 확인)
정적 파일이라 아무 로컬 서버로 열면 됩니다:
```
python3 -m http.server 8000
# 또는  npx serve .
```
브라우저에서 http://localhost:8000 → 각 페이지 확인.

## 배포
`README.md`의 단계(GitHub → Vercel → 후이즈 DNS → 구글 워크스페이스 메일)를 따르세요.

## 콘텐츠 원본 (참고)
문구·실적·브랜드 상세는 claude.ai 프로젝트 "홈페이지 제작"의 문서 참고:
`greenpro-site-plan.md`(기획안), `greenpro-website-handoff.md`(인수인계), `greenpro-build-status.md`(진행상태), `greenpro-privacy-policy.md`(개인정보처리방침 전문).
