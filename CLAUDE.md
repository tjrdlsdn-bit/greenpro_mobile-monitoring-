# CLAUDE.md — 그린프로 홈페이지 (Claude Code 작업 지침)

이 파일은 Claude Code가 이 프로젝트를 이어받을 때 **가장 먼저 읽는 규칙서**입니다.
사이트는 이미 완성되어 **Vercel에 배포 중**입니다. 처음부터 다시 만들지 말고, 기존 코드를 이어서 다듬으세요.

> 최종 업데이트: 2026-09-10

---

## 1. 프로젝트 개요

- **회사**: ㈜그린프로 (GREENPRO) — 태양광 신재생에너지 **정부지원사업 전문 컨설팅**
- **한 줄**: 배출권거래제 할당대상업체를 위해, 태양광 지원사업을 신청부터 사후관리까지 대행하는 밀착 컨설팅
- **주 타깃**: 배출권거래제 할당대상업체(중소·중견). 확대: 배출권 비해당 기업
- **목적**: B2B 리드(무료 상담) 수집

## 2. 현재 배포 상태

| 항목 | 값 |
|---|---|
| GitHub | https://github.com/tjrdlsdn-bit/greenpro_mobile-monitoring- |
| 배포 주소(고정) | https://greenpro-mobile-monitoring.vercel.app/ |
| 배포 방식 | GitHub `main` 브랜치에 push → Vercel 자동 재배포 (1~2분) |
| 최종 도메인 | greenpro.co.kr (아직 연결 전 — 후이즈에서 DNS 작업 필요) |
| 커밋 작성자 | 석인우 / tjrdlsdn@gmail.com |

**작업 후에는 반드시 commit + push** 해야 배포 주소에 반영됩니다.

---

## 3. 반드시 지킬 것 (Rules)

### 브랜드
- **메인 컬러는 블루 `#0049F0`** (회사명은 '그린'이지만 시각 정체성은 블루). 그린 계열로 바꾸지 말 것
- **폰트는 Pretendard** (`style.css`에서 CDN import). **검은고딕(Black Han Sans) 사용 금지** — 숫자 포함 전부 Pretendard
- **"규모가 작아 보이지 않게"** — 넓은 여백, 큰 타이포, 실적 숫자를 전면에. "중소·작은" 류 표현 노출 금지
- 디자인 톤: 신뢰·전문·안심·전담 (모던·미니멀 + 얇은 라인 + 큰 워드마크)
- **AI스러운 디자인 금지**: 보라 그라디언트, 과한 그림자, 둥근 모서리 남발, 이모지 장식

### 카피 스타일 (작업 중 확정된 규칙)
- **제목에는 쉼표·마침표를 쓰지 않는다**. 예: "복잡한 지원사업을 / 가장 단순하게" (O), "복잡한 지원사업을, / 가장 단순하게." (X)
- 단, 숫자의 천 단위 쉼표(20,000kW)는 유지
- **"20년" 같은 구체적 기간을 히어로에 노출하지 않는다** — "신청부터 사후관리까지"로 표현 (다른 섹션의 20년 표기는 유지)
- 줄바꿈은 `<br>`로 명시적으로 지정 (문장이 어중간하게 끊기지 않도록)

### 반응형
- **완전 반응형** 유지 (모바일 우선). 브레이크포인트 **900px / 640px** 준수
- 모바일(640px 이하)에서 메뉴는 햄버거로 접히지만 **"무료 상담" 버튼은 계속 노출**시킬 것 (문의 전환율)

---

## 4. 기술 스택 / 구조

- **순수 정적 사이트** (HTML/CSS/JS, 프레임워크·빌드 도구 없음)
- 페이지 6개: `index.html`(홈) · `about.html`(회사소개) · `business.html`(사업영역) · `record.html`(실적·사례) · `contact.html`(문의) · `privacy.html`(개인정보처리방침)
- 공통 파일: `assets/css/style.css`, `assets/js/main.js`
- **헤더·푸터·상단배너는 각 페이지에 인라인으로 중복**되어 있음 → 수정 시 **6개 파일 전부 동일하게 반영**할 것
- 이미지: `assets/img/` (favicon.svg 있음)
- SEO: `sitemap.xml`, `robots.txt` (루트에 위치, greenpro.co.kr 기준으로 작성됨)

### main.js 구성 (4개 블록)
1. `history.scrollRestoration = 'manual'` — 새로고침 시 항상 첫 화면부터
2. **원페이지 스크롤** — 홈에서만 동작. 휠 한 번에 한 섹션씩 이동
3. 모바일 햄버거 메뉴 토글
4. 숫자 카운트업 애니메이션 (소수점 지원 — `data-to="2.7"` 같은 값 가능)
5. 문의 폼 전송 + 허니팟 스팸 차단

---

## 5. 회사 정보 (푸터·문의·개인정보처리방침 공통)

- ㈜그린프로 / 대표전화 010-2293-7342 / info@greenpro.co.kr
- 주소: 충남 천안시 서북구 2공단1로 17 천안자이타워 지식산업센터 1105호
- 사업자등록번호: 791-81-00084
- 개인정보 보호책임자: 석동호 이사 (시행일 2026-09-02)

---

## 6. 완료된 작업

- [x] **문의 폼 실제 전송** — Formspree 연결 (`main.js`의 `FORM_ENDPOINT`)
- [x] **스팸 방지** — 허니팟 필드(`_gotcha`) 적용. 봇이 채우면 전송하지 않고 성공한 것처럼만 표시
- [x] **Favicon** — `assets/img/favicon.svg` (블루 배경 + 흰색 G), 6개 페이지 전부 적용
- [x] **개인정보처리방침 최종화** — 보호책임자·시행일 확정
- [x] **SEO 기본** — sitemap.xml, robots.txt, 페이지별 title/description/OG
- [x] **GitHub + Vercel 배포**
- [x] 히어로 첫 화면 최적화 (화면 높이에 자동으로 맞춰짐)
- [x] 통계 3종 카드형 디자인 (70% / 20,000kW / 2.7년), 숫자=파랑·단위=검정
- [x] 원페이지 스크롤 효과 (휠 한 번 = 한 섹션)
- [x] 상단 배너 파란 배경 + 『겹낫표』 강조

## 7. 아직 안 된 것 (우선순위 순)

1. **시공 사진 교체** ⭐ 가장 시급
   `index.html`·`record.html`에 `[ 시공 사진 ]` 플레이스홀더 6곳. 실제 사진 확보 후 교체.
   ※ 기존 호스팅(지오디웹스) 서버에 있던 사진 자료 회수 필요 — 삭제 기한 주의
2. **Formspree 확인 이메일 클릭** — 첫 제출 건에 대해 Formspree가 확인 메일을 보냄. 그 링크를 눌러야 실제 문의가 이메일로 전달되기 시작함
3. **도메인 연결** — Vercel Settings → Domains에 greenpro.co.kr 추가 → 후이즈(whois.co.kr)에서 A/CNAME 등록
   ⚠️ **"그린프로" 구글 검색 1위 자산 보존이 중요** — 도메인 유지, URL 구조·메타 크게 바꾸지 말 것
4. **회사 이메일 개설** — 구글 워크스페이스로 info@greenpro.co.kr. **DNS 연결하는 시점에 MX/TXT를 같이 등록** (따로 하지 말 것)
5. **사업영역 "대상별 안내" 섹션 재작성** — 기존 섹션은 삭제된 상태. 정리 후 다시 만들 예정
6. 실적 표(5개)와 사진 카드(3개) 개수 불일치 — 사진 확보 후 5개로 맞추기 검토
7. 문의 폼 이메일 필드가 선택사항 — 드론 3D 분석 자료 전달을 생각하면 필수로 변경 검토
8. (선택) Google Analytics

---

## 8. 작업 시 주의사항 (실제로 겪은 함정)

### 인라인 style이 CSS를 덮어쓴다
HTML에 `style="..."` 로 박힌 속성은 `style.css`의 규칙보다 우선합니다.
CSS를 아무리 고쳐도 화면이 안 바뀌면 **해당 요소의 인라인 style부터 확인**하세요.
(실제로 `<section class="hero sec" style="padding-bottom:0">` 때문에 여백 조정이 계속 먹히지 않았던 적 있음)

### 로컬 미리보기 캐시
로컬 서버로 확인할 때 브라우저가 이전 `main.js`/`style.css`를 캐시해서 수정이 반영 안 될 수 있습니다.
변경이 안 보이면 강력 새로고침(Ctrl+Shift+R) 또는 서버 재시작.

### 헤더/푸터 중복
정적 사이트라 헤더·푸터·상단 배너가 6개 HTML에 각각 들어있습니다.
문구 하나 바꿔도 **6곳 모두** 수정해야 합니다. (CSS는 공통이라 한 번만 고치면 됨)

### 원페이지 스크롤에서 특정 섹션 제외하기
내용이 길어서 자동 이동이 불편한 섹션은 `<section>` 태그에 **`data-freescroll`** 속성을 붙이면
그 구간에서는 자동 이동 없이 평소처럼 스크롤됩니다. (현재 실적 섹션에 적용됨)

---

## 9. 로컬에서 확인하기

```bash
npx serve .
# 또는
python3 -m http.server 8000
```
→ 브라우저에서 http://localhost:8000

## 10. 다른 컴퓨터에서 이어받기

```bash
git clone https://github.com/tjrdlsdn-bit/greenpro_mobile-monitoring-.git
```
- 첫 push 때 GitHub 로그인 창이 한 번 뜹니다
- 커밋 작성자 설정이 필요할 수 있습니다:
  ```bash
  git config user.name "석인우"
  git config user.email "tjrdlsdn@gmail.com"
  ```
- Vercel은 GitHub에 연결돼 있어 어느 컴퓨터에서 push하든 자동 배포됩니다

## 11. 참고 문서

`docs/` 폴더 안에 있습니다.
- `greenpro-site-plan.md` — 페이지별 섹션 구성·문구 기획 근거
- `greenpro-handoff.md` — 인프라(도메인·호스팅·SEO) 현황과 결정사항
- `greenpro-privacy-policy-full.md` — 개인정보처리방침 전문 초안
